{
  "name": "downshift-example-with-error-support",
  "version": "0.0.77",
  "description": "downshift doesn't manage error state for you, instead, you can hook into whenever the input value changes with the onStateChanged handler and validate the input yourself. Then you can set the error state yourself and render differently based on the error state.",
  "keywords": [
    "downshift:examples"
  ],
  "main": "index.js",
  "dependencies": {
    "@babel/core": "7.2.2",
    "@babel/preset-env": "7.3.1",
    "@emotion/core": "10.0.6",
    "@emotion/styled": "10.0.6",
    "babel-plugin-emotion": "10.0.6",
    "downshift": "1.0.0-rc.5",
    "match-sorter": "2.3.0",
    "react": "16.7.0",
    "react-dom": "16.7.0",
    "starwars-names": "1.6.0"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}